name: Self

on:
  pull_request:
    branches:
      - master

jobs:
  self:
    name: TEST
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Check out the repository
        uses: actions/checkout@v2

      - name: 👷 Build wheels
        uses: ./
        with:
          test: true

      - name: 👷 Build wheels
        uses: ./
